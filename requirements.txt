# Web Framework & Server
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Database
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9

# Utils
pydantic==2.6.1
slowapi==0.1.9
python-dotenv==1.0.1

# IA (informe PRO/PREMIUM vía DeepSeek — cliente compatible OpenAI)
# Sin esta dependencia, /v3/chat-analysis devuelve 503 silenciosamente.
# >=1.55 requerido: versiones anteriores rompen con httpx 0.28 (kwarg proxies).
openai==1.59.7

# Cache distribuida (opcional: solo si REDIS_URL está configurada)
redis==5.0.4